# SimpleCRUD

SimpleCRUD is a .NET 8 Web API project built to manage users, applications, roles, and their relationships through a layered backend architecture.

This repository is focused on the API, business logic, Entity Framework Core data context, database entities, and migrations. The UI is intentionally kept in a separate repository and should communicate with this API through HTTP requests only.
